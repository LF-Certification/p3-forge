name: Dev build

on:
  push:
    paths:
      - 'images/**'
    branches:
      - '**'

env:
  REGISTRY: ghcr.io
  REGISTRY_USER: ${{ github.actor }}
  REGISTRY_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      changed-images: ${{ steps.changes.outputs.changed-images }}
      has-changes: ${{ steps.changes.outputs.has-changes }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install devbox
        uses: jetify-com/devbox-install-action@v0.13.0
        with:
          enable-cache: "true"
          devbox-version: "0.14.2"

      - name: Run linting
        run: |
          devbox run -- make lint

      - name: Detect changed images
        id: changes
        env:
          GITHUB_EVENT_BEFORE: ${{ github.event.before }}
        run: |
          RESULT=$(devbox run -- make ci-detect-changed-images-json)

          HAS_CHANGES=$(echo "$RESULT" | jq -r '.has_changes')
          CHANGED_IMAGES=$(echo "$RESULT" | jq -c '.changed_images')

          echo "has-changes=$HAS_CHANGES" >> $GITHUB_OUTPUT
          echo "changed-images=$CHANGED_IMAGES" >> $GITHUB_OUTPUT

          if [ "$HAS_CHANGES" = "true" ]; then
            echo "Changed images detected:"
            echo "$CHANGED_IMAGES" | jq -r '.[]'
          else
            echo "No changes detected in images/"
          fi

  build:
    runs-on: ubuntu-latest
    needs: detect-changes
    if: needs.detect-changes.outputs.has-changes == 'true'
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        image-path: ${{ fromJson(needs.detect-changes.outputs.changed-images) }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install devbox
        uses: jetify-com/devbox-install-action@v0.13.0
        with:
          enable-cache: "true"
          devbox-version: "0.14.2"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ env.REGISTRY_USER }}
          password: ${{ env.REGISTRY_TOKEN }}

      - name: Extract image name from path
        id: extract
        run: |
          IMAGE_NAME=$(basename "${{ matrix.image-path }}")
          echo "image-name=$IMAGE_NAME" >> $GITHUB_OUTPUT

      - name: Generate image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ steps.extract.outputs.image-name }}
          tags: |
            # Dev branch-latest tag
            type=ref,event=branch,prefix=dev-,suffix=-latest
            # Dev branch-sha tag
            type=sha,prefix=dev-{{branch}}-
            # Dev-latest tag for main branch only
            type=raw,value=dev-latest,enable={{is_default_branch}}
          labels: |
            org.opencontainers.image.title=${{ steps.extract.outputs.image-name }}
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}

      - name: Generate Java variant metadata
        id: java-meta
        if: steps.extract.outputs.image-name == 'p3-sandbox-ide'
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ steps.extract.outputs.image-name }}
          tags: |
            type=ref,event=branch,prefix=dev-,suffix=-latest-java
            type=sha,prefix=dev-{{branch}}-,suffix=-java
            type=raw,value=dev-latest-java,enable={{is_default_branch}}
          labels: |
            org.opencontainers.image.title=${{ steps.extract.outputs.image-name }}
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}

      - name: Build and push container image
        uses: docker/build-push-action@v5
        with:
          context: ${{ matrix.image-path }}
          file: ${{ matrix.image-path }}/Dockerfile
          target: ${{ steps.extract.outputs.image-name == 'p3-sandbox-ide' && 'base' || '' }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          platforms: linux/amd64,linux/arm64
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: false

      - name: Build and push Java variant
        if: steps.extract.outputs.image-name == 'p3-sandbox-ide'
        uses: docker/build-push-action@v5
        with:
          context: ${{ matrix.image-path }}
          file: ${{ matrix.image-path }}/Dockerfile
          target: java
          push: true
          tags: ${{ steps.java-meta.outputs.tags }}
          labels: ${{ steps.java-meta.outputs.labels }}
          platforms: linux/amd64,linux/arm64
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: false

      - name: Output build summary
        run: |
          echo "## 🚀 Dev Build Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Image:** \`${{ steps.extract.outputs.image-name }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Source:** \`${{ matrix.image-path }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Branch:** \`${{ github.ref_name }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📦 Published Tags:" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "${{ steps.meta.outputs.tags }}" | tr ',' '\n' >> $GITHUB_STEP_SUMMARY
          if [[ -n "${{ steps.java-meta.outputs.tags }}" ]]; then
            echo "${{ steps.java-meta.outputs.tags }}" | tr ',' '\n' >> $GITHUB_STEP_SUMMARY
          fi
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
